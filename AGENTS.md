<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes â€” APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Notion Canon v2 — operative project instructions (2026-08-04)

Conflicting v1 strategy statements above are superseded and retained only as history.

### Current

The implemented project is a static Next.js App Router export using repository Markdown, without database, auth, payments, account, comments, CMS, API, backend runtime, workers, or queues.

### Approved direction

The website is an authority hub and canonical content/research source with a future independent commercial branch, while Pro-leads and Tender Audit remain separate products. Use the conceptual content types `note`, `article`, `case`, `research`, `product material`, and `paid material` for strategy; do not mutate the current front-matter contract without a separate specification.

### Future / Gate

Before work on server state, require Gate 2 commercial validation: about 5–10 target-user payments outside Maxim's immediate circle plus clear purchase reasons. A separately approved Gate 3 may add only registration, one-time purchases, purchase access, and a minimal purchases/access account. Later functions require their own Gate evidence, success criteria, allowed scope, exclusions, and stop or rollback condition. Telegram Mini App is non-priority; native applications are Gate 7.

### Out of scope

Future documentation is never authorization to implement backend, auth, payment, database, account, comments, community, marketplace, PWA, Telegram Mini App, native apps, or shared product infrastructure. Do not merge products or change code, production, deployment, dependencies, routes, configuration, or content without an explicit task.
